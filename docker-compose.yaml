service:
  simple-apps:
    container_name: ct-simpleapps
    # build: /app
    image: apeengggg/simpleapps
    ports:
      - "8181:3000"
    # env_file:
    #   - ./app/.env
    environment:
      - DB_NAME=training
      - DB_HOST=172.23.8.28
      - DB_USER=peserta
      - DB_PASSWORD=password
      - APP_PORT=3000
    restart: always